1.変数の定義
Javaの中に基本データ型,参照データ型
基本データ型はINT,CHAR,BYTE,BOOLEANなどの型に分けられる
参照データ型はstring、クラス、interfaceに分けられる
２.演算子
3.制御構文
条件分岐：if, else, switch
ループ：for, while, do-while
4.配列
参照データ型
５．カプセル化
privateを使ってプロパティをカプセル化、外部から直接にアクセスできない、GETTER,SETTERを通してプロパティを変更したりアクセスしたりする
６．継承
javaの単一継承、EXTENDSを使って
７，ポリモーフィズム（多態性）
対象のポリモーフィズム：コンパイル時の型、実行時の型（動的型）メソッドのポリモーフィズム：オーバーライド、オーバーロード
８．抽象的クラス
abstract でクラスとメソッドを修飾する，継承てきますが、インスタンス化できません、抽象メソッドを含む、通常のメソッドやフィールドを持つこともできる
９．interface
すべてのメソッドをpublic abstractで修飾する、具体的な実装を持ちません、プロパテはpublic STATICで修飾する、直接にアクセスすることができる
インターフェースは複数継承することができます
１０．例外処理
例外は运行时异常、编译时异常、エールに分けられる、例外処理は二つがあります、一つはメソッドの中にtry-catch-finallyを使用して
もう一つはメソッドの定義の時にthrowsを使用して
１１．THIS,superの違い
thisは一般的にクラス自身を指し、superは親クラスを指すために使用されます
１２．final
finalで修飾されたクラスは継承できず、修飾されたメソッドはオーバーライドできません。
修飾されたフィールド（属性）は変更することができません。
１３．常用类
math、arrays、
14.集合
重複を許可し、順序を必要するのは、listコレクションを使用します
検索と修正の場合が多いArrayListを使って
加入と削除の場合が多いLinkedListを使って
スレッドセーフが必要　Vectorを使って

重複を許可しない場合はsetを使用します
順調が必要の場合treesetを使って
入力と検索の順番が同じな場合はlinkedhashset
順番がないの場合はhashsetを使って

キーと値のペアの場合mapを使って
キーが順調がない場合hashmapを使って
キーが順調がある場合treemapを使って
キーは入力と検索のときの順番が同じな場合はlinkedhashmap
map
hashmap、treemap、hashtable
15.泛型（ジェネリクス）
1.コレクションを作成する際に使用され、コレクションに入力されたデータの型を制限します
２.クラスを作成する際に、中にある変数の型はまだ決められない場合は、ジェネリクスを使って、インスタントを作成する時に、型を決める
